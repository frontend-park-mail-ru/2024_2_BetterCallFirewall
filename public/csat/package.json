{
  "name": "csat",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "prestart": "npm run compile:templates && npm run compile:bundle",
    "start-csat": "npm run prestart && node ./server/server.js",
    "compile:templates": "find components/ -name '*.hbs' | xargs handlebars -f ./precompiled.js",
    "compile:bundle": "npx webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
